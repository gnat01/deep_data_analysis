# SQL Notes

This directory is reserved for:

- warehouse table definitions
- analytical views
- repeatable reporting queries
- PostgreSQL knowledge-base schema artifacts

It should stay aligned with the canonical schema described in `../docs/schema.md`.

Current Step 18 artifact:

- `postgres_schema.sql`: PostgreSQL schema for loading the V1 core tables into a queryable knowledge base
