# SQL Notes

This directory is reserved for:

- warehouse table definitions
- analytical views
- repeatable reporting queries

It should stay aligned with the canonical schema described in `../docs/schema.md`.
